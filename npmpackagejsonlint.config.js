const config = {
  rules: {
    'prefer-absolute-version-dependencies': 'error', // dependencies のバージョンを絶対パスに統一
    'prefer-absolute-version-devDependencies': 'error', // devDependencies のバージョンを絶対パスに統一
  },
};

module.exports = config;
